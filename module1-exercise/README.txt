Assignment 1

- The empty strings between commas are not considered (cycles the array and checks for a not empty string, maybe this can be done with angular instead of js)
- adds a class to the div in order to display the message in different colors
